module.exports = {
    siteUrl: `https://www.tamilimages.xyz`, // Site domain. Do not include a trailing slash!

    postsPerPage: 9, // Number of posts shown on paginated pages (changes this requires sometimes to delete the cache)

    siteTitleMeta: `Tamil Images`, // This allows an alternative site title for meta data for pages.
    siteDescriptionMeta: `Tamil Images - Tamil SMS, Kavithai and Quotes Images for Social Status.`, // This allows an alternative site description for meta data for pages.

    shareImage: `/images/banner.jpg`, // fallback image for meta data. Will be used, when no post/tag/author image specified. 1200x1200 is recommended
    shareImageWidth: 1000, // Change to the width of your default share image
    shareImageHeight: 523, // Change to the height of your default share image

    shortTitle: `Tamil Images`, // Used for App manifest e.g. Mobile Home Screen
    siteIcon: `favicon.png`, // Logo in /static dir used for SEO, RSS, and App manifest
    backgroundColor: `#2b2e3c`, // Used for Offline Manifest
    themeColor: `#3498DB`, // Used for Offline Manifest

    googleAnalyticsID: `UA-67506568-18`, // Google Analytics
    siteFBAppID: `371109687084744`,
}
